{
  "name": "git-extract-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "lint": "eslint --ext .js src/"
  },
  "dependencies": {
    "itty-router": "^5.0.18",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "wrangler": "^3.57.0"
  }
}
